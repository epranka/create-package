language: node_js
os: linux

jobs:
  include:
    # Define the release stage that runs semantic-release
    - stage: release
      name: "Releasing"
      node_js: lts/*
      deploy:
        provider: script
        cleanup: false
        script: npx semantic-release
